# Copy this file to `keystore.properties` (gitignored) and fill in your values
# to enable signed release builds. Never commit the real file or the .keystore.
storeFile=/absolute/path/to/poochydex-release.keystore
storePassword=CHANGE_ME
keyAlias=poochydex
keyPassword=CHANGE_ME
